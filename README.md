# adamreidelbach.com

Adam Reidelbach's personal landing page.

The site is intentionally plain HTML and CSS with no framework, package
manager, runtime, database, authentication, or build step.

To preview it locally, open `index.html` in a browser or serve the directory
with any static file server.

GitHub Pages can publish the repository directly from the branch root.
